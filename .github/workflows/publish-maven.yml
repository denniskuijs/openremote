name: Maven
on:
  workflow_call:
    secrets:
      MAVEN_SIGNING_PASSWORD:
        required: true
      MAVEN_SIGNING_KEY:
        required: true
      MAVEN_USERNAME:
        required: true
      MAVEN_PASSWORD:
        required: true

jobs:
  maven-publish:
    name: Publish to Maven Central Sonatype
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up JDK 21 and Gradle cache
        uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5.5.0
        with:
          distribution: "temurin"
          java-version: "21"
          cache: "gradle"

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: or-manager-build
          path: manager/build

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: or-ui-component-build
          path: ui/component

#      - name: Publish to Maven Central Sonatype
#        env:
#          # Gradle automatically converts ORG_GRADLE_PROJECT_x into -Px
#          ORG_GRADLE_PROJECT_signingKey: ${{ secrets.MAVEN_SIGNING_KEY }}
#          ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.MAVEN_SIGNING_PASSWORD }}
#          ORG_GRADLE_PROJECT_publishUsername: ${{ secrets.MAVEN_USERNAME }}
#          ORG_GRADLE_PROJECT_publishPassword: ${{ secrets.MAVEN_PASSWORD }}
#        run: ./gradlew publishToSonatype --no-parallel
#      - name: Close and Verify Staging Repository
#        # This will poll the Sonatype API and wait until all validation rules pass.
#        run: ./gradlew findSonatypeStagingRepository closeSonatypeStagingRepository
#        env:
#          ORG_GRADLE_PROJECT_publishUsername: ${{ secrets.MAVEN_USERNAME }}
#          ORG_GRADLE_PROJECT_publishPassword: ${{ secrets.MAVEN_PASSWORD }}
#      - name: Release to Maven Central Sonatype
#        env:
#          ORG_GRADLE_PROJECT_publishUsername: ${{ secrets.MAVEN_USERNAME }}
#          ORG_GRADLE_PROJECT_publishPassword: ${{ secrets.MAVEN_PASSWORD }}
#        run: ./gradlew findSonatypeStagingRepository closeAndReleaseSonatypeStagingRepository
